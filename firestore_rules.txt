rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write to demo_test collection (for testing)
    match /demo_test/{document=**} {
      allow read, write: if true;
    }
    
    // Allow read/write to tenants collection (for your device data)
    match /tenants/{tenant_id}/{document=**} {
      allow read, write: if true;
    }
    
    // Allow read/write to installations collection (for device installations)
    match /installations/{document=**} {
      allow read, write: if true;
    }
    
    // Allow read/write to test collection (if you need it)
    match /test/{document=**} {
      allow read, write: if true;
    }
  }
}

