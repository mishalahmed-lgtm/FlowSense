import { useAuth } from "../context/AuthContext.jsx";

export default function Navbar() {
  // Navbar is now redundant with the modern sidebar - rendering nothing
  // All navigation is handled by the Sidebar component
  return null;
}


